#include <stdio.h>

/**
 * main - printint strings
 * Description: using printf
 * Return: Always 0 (success)
 */

int main(void)
{
	printf("%s", "with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
